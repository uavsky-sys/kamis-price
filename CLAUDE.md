# 농산물 가격 (5번 프로젝트)

- 코드 위치: `E:\코딩200\농수산물 오늘가격` · 환경 🟩본방
- GitHub **공개** 저장소 `uavsky-sys/kamis-price` — **master 푸시 = Cloudflare Workers 자동 배포**
- 사이트: auction-price.uavsky.workers.dev (단일 `public/index.html`)
- 워커 3기능: ①메인=공공데이터 경매 프록시 ②`/garak`=가락 직접조회 백업 ③`/trend`=시세 추이 그래프
  - 소스 `cloudflare-worker.js` ↔ `worker/index.js` 동기화 필요 (`worker/`는 gitignore)
- Play 스토어 TWA: 패키지 `com.uavsky.nongsansise`, 앱명 "농산물 가격", 버전 2(1.1.0.0) Alpha 게시 완료

## ⚠️ 절대 지킬 것

- **공개 저장소다.** API키·keystore·`worker/` 폴더 절대 커밋 금지 (gitignore로 막아 뒀음)
- 커밋 메시지에 큰따옴표 금지 (PowerShell 5.1이 인자를 쪼갬 — 2회 재발한 사고)
- `wrangler secret` 값은 파일로 주입할 것. 붙여넣으면 줄바꿈이 섞여 401 남
- 한글 든 파일을 PowerShell `-replace`로 일괄 수정 금지 (인코딩 깨짐) — Edit/Write 도구 사용

---

### 🚦 막히면 이렇게 한다 (지휘 본부 규약)

공용 파일 두 개가 구글 드라이브 `G:\내 드라이브\비서실장_보고함\` 에 있다.

| 파일 | 무엇 |
|---|---|
| `현황판.md` | 프로젝트 9개 신호등. **내 줄만** 고친다 |
| `대장몫.md` | 사람만 할 수 있는 일. 여기 없는 건 내가 한다 |

**막혔을 때 세 줄:**

1. **`현황판.md` 에서 내 줄의 상태를 바꾸고 「무엇을 기다리나」를 채운다**
   - 🟡답대기 = 답을 기다리지만 **다른 일은 할 수 있다**
   - 🔴막힘 = **답 없이는 이 프로젝트에서 한 줄도 못 쓴다**
   - ⚠️ 「대신 하는 일」이 하나라도 있으면 그건 🔴이 아니라 🟡이다. 🔴은 아껴 쓴다

2. **🔴이면 `급질문_YYYYMMDD_프로젝트.md` 를 보고함에 낸다**
   - 급행 순찰이 **2시간 안에** 집어서 대장 폰으로 알린다
   - 🟡이면 평소대로 `질문_YYYYMMDD.md` 로 낸다 (다음 정기 순찰, 최대 6시간)
   - 급질문에는 **「예/아니오로 답할 수 있는 질문 하나」**만 쓴다. 갈래마다 내가 뭘 할지도 미리 적는다

3. **멈춰 서 있지 않는다 — 「대신 하는 일」을 골라 적고 그걸 한다**
   - 미리 정해 둔 목록에서 고른다 (아래 「내 프로젝트의 대신 하는 일」)
   - 고른 것을 `현황판.md` 의 내 줄에 적는다. 본부가 "지금 뭘 하고 있나"를 알 수 있게

**풀렸으면 내가 되돌린다.** 🟢으로 바꾸고, 대장이 해 주신 일이면 `대장몫.md` 의 그 줄을 「끝난 것」으로 옮긴다. 본부가 대신 안 해 준다.

**사람만 할 수 있는 일이 생기면** `대장몫.md` 에 한 줄 추가한다. 반드시 **예상 분**과 **왜 대장 몫인지**를 적는다. "왜 대장 몫인가"를 못 쓰겠으면 그건 내가 할 수 있는 일이다.

**낡은 문서를 발견하면** `현황판.md` 맨 아래 「고쳐야 할 문서」에 한 줄 적는다. 남이 그것 때문에 또 시간을 태우지 않게.

### 내 프로젝트의 「대신 하는 일」 3개

밖(증권사·API·대장 판단)을 안 타는 일만 고른다. 세션이 처음 켜질 때 이 셋을 채워 두고, 하나 쓰면 하나 채운다.

1. **워커 두 벌 어긋남 점검** — `cloudflare-worker.js`와 `worker/index.js`가 같은 내용인지 대조한다. 어긋난 채로 푸시하면 사이트가 옛 코드로 배포되는 사고가 난다. 대조·정리만 하고 배포는 안 한다.
2. **미커밋 스토어 자료 7개 정리** — 스크린샷 3장·그래픽이미지·앱아이콘·등록정보 초안·테스터 모집 안내가 git에 안 올라간 채 떠 있다. 커밋할 것과 `.gitignore`로 뺄 것을 갈라 정리한다 (공개 저장소라 개인정보·키가 찍힌 화면이 없는지 함께 확인).
3. **낡은 문서 점검** — `테스터_모집_안내.md`·`스토어_등록정보_초안.md`가 지금 상태(앱명 "농산물 가격", 버전 1.1.0.0, 테스터 12/12 달성)와 맞는지 대조해 고친다. 밖에 안 나가는 내부 문서라 대장 승인 없이 고칠 수 있다.
